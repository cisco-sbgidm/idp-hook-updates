{
  "name": "idp-hook-updates",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -Rf dist",
    "zip": "yarn clean ; tsc ; yarn install --prod --modules-folder dist/node_modules ; (cd dist; zip -r idp-hook-updates .;)",
    "test": "jest",
    "lint": "tslint -c tslint.json src/*.ts",
    "lint:specs": "tslint -c tslint.json src/__tests__/*.ts"
  },
  "dependencies": {
    "axios": "^0.19.1",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "@types/jest": "^25.1.1",
    "@types/lodash": "^4.14.149",
    "@types/node": "^13.5.0",
    "aws-sdk": "^2.610.0",
    "husky": "^4.0.7",
    "jest": "^24.9.0",
    "jest-extended": "^0.11.2",
    "ts-jest": "^25.1.0",
    "tslint": "^6.0.0",
    "tslint-config-airbnb": "^5.11.2",
    "typescript": "^3.7.5"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn lint ; yarn lint:specs"
    }
  }
}
