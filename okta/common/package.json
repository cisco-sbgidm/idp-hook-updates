{
  "name": "okta-common",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "tsc && tscpaths -p tsconfig.json -s ./src -o ./dist ; sed -i '' 's+../../../++' `find dist -name \"*.js\"`",
    "clean": "rm -Rf dist",
    "test": "yarn clean ; jest",
    "lint": "tslint -c ../../tslint.json src/*.ts src/**/*.ts",
    "setup": "yarn tsc ; node ./dist/okta/common/scripts/SetupOkta.js"
  }
}
