{
  "name": "okta-duo-aws",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -Rf dist",
    "test": "yarn clean; jest",
    "zip": "yarn clean ; tsc ; yarn install --prod --modules-folder dist/node_modules ; (cd dist; zip -r --symlink idp-hook-updates .;)",
    "lint": "tslint -c tslint.json src/*.ts src/**/*.ts"
  }
}
