{
  "name": "common-duo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "yarn clean; ttsc",
    "clean": "rm -Rf dist",
    "test": "jest",
    "lint": "tslint -c ../../../tslint.json src/*.ts src/**/*.ts",
    "duo:manageAdminAPI": "yarn run ts-node -r tsconfig-paths/register scripts/ManageDuoAdminAPI.ts"
  }
}
