{
  "name": "common-duo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -Rf dist",
    "test": "jest",
    "lint": "tslint -c tslint.json src/*.ts src/**/*.ts",
    "duo:manageAdminAPI": "yarn ts-node scripts/ManageDuoAdminAPI.ts"
  }
}
