{
  "name": "common-gcp",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "yarn clean; ttsc",
    "clean": "rm -Rf dist",
    "test": "yarn clean; jest",
    "lint": "tslint -c ../../../tslint.json src/*.ts src/**/*.ts"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^2.0.0"
  }
}
