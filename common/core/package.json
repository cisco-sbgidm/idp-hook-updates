{
  "name": "common-core",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -Rf dist",
    "test": "yarn clean; jest",
    "lint": "tslint -c ../../tslint.json src/*.ts src/**/*.ts"
  },
  "dependencies": {
    "redis": "^3.0.2"
  }
}
