{
  "name": "common-core",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -Rf dist",
    "test": "echo no tests in this project",
    "lint": "tslint -c ../../tslint.json src/*.ts src/**/*.ts"
  }
}
